from pathlib import Path

# Directory paths
project_dir = str(Path('.').resolve())
img_dir = str(Path('.').resolve() / 'reports' / 'figures')
data_external_dir = str(Path('.').resolve() / 'data' / 'external')
data_synthetic_dir = str(Path('.').resolve() / 'data' / 'synthetic')
